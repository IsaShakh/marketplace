import CustomButton from "host/Button";
import Layout from "host/Layout";

const MarketplaceComponent = () => {
  return (
    <Layout>
      <h1>Marketplace Component</h1>
      <CustomButton>тест</CustomButton>
    </Layout>
  );
};

export default MarketplaceComponent;
